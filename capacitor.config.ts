import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sycamoresstake.5thward',
  appName: 'Sycamores 5th Ward App',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
